version: 2

sources:
  - name: redshift_schema
    schema: schema
    database: database
    loader: server

    tables:
      - name: source_company_customers
        description: Represents a customer
        columns:
          - name: id
            tests:
              - not_null
              - unique

      - name: source_sales_transactions
        description: Represents a sales transation
        columns:
          - name: id
            tests:
              - not_null
              - unique
