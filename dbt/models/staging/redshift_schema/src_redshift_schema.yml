version: 2

sources:
  - name: redshift_schema
    schema: schema
    database: database
    loader: server

    tables:
      - name: dim_customer
        description: Represents a customer
        columns:
          - name: id
            tests:
              - not_null
              - unique

      - name: fact_sales
        description: Represents a sales transation
        columns:
          - name: id
            tests:
              - not_null
              - unique
