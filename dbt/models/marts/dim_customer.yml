version: 2

models:
    - name: dim_customer
      description: >
        customer dimension providing descriptive information across all customers.
      columns:
        - name: customer_id
          description: >
            Unique company id for each customer.
          tests:
            - not_null
            - unique
