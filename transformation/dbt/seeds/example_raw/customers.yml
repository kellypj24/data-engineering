version: 2

seeds:
  - name: customers
    description: >
      Example fixture standing in for raw.customers. Delete seeds/example_raw/
      in a real project.
    config:
      meta:
        owner: data-platform
      column_types:
        id: integer
    columns:
      - name: id
        description: Primary key
        tests:
          - unique
          - not_null
